Title

Run a Legacy PHP 5.6 App Using Docker (PHP 5.6 + MySQL 5.7 + phpMyAdmin)

Features

PHP 5.6 (EOL) supported using Docker

Apache server

MySQL 5.7 database

phpMyAdmin connected to MySQL

Autoload SQL initialization

Hot reload via volume mounting

Zero host setup — only Docker needed

How to Run
git clone
cd dockerize-legacy-php5.6
docker-compose up --build